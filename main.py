from tsp import TSP
from pathlib import Path

CURRENT_DIR = Path.cwd()
DIR_INSTANCES = CURRENT_DIR / "instancias"
DIR_INSTANCES_S = CURRENT_DIR / "instances" / "small"
DIR_INSTANCES_M = CURRENT_DIR / "instances" / "medium"
DIR_INSTANCES_L = CURRENT_DIR / "instances" / "large"


small_instances = []

if __name__ == "__main__":
    # Ejemplo de uso
    
    print("Test")
